{
  "name": "agentic-os",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/bridge",
    "apps/dashboard"
  ],
  "scripts": {
    "clean:dev-cache": "node scripts/clean-dev-cache.js",
    "dev": "node scripts/clean-dev-cache.js && concurrently --kill-others-on-fail \"npm run dev --workspace=apps/bridge\" \"npm run dev --workspace=apps/dashboard\"",
    "build": "npm run build --workspace=apps/bridge && npm run build --workspace=apps/dashboard",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
